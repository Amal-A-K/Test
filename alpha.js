const string="welcome";
function ans(s){
    const arr=string.split("");
    const alpha=arr.sort();
    console.log(alpha);
}
ans(string);

const strings=["car","ant","well","dell"];
function answer(x){
    console.log(x.sort());
} 
answer(strings);